# Track B (Workstream 3): GRU sequence model over proposal cards with a
# 15-way evil-pair head. Same card inputs as Track A (shared
# proposal_cards.py builder — no separate tokenizer to maintain); the A/B
# comparison isolates (a) pooling vs recurrence and (b) independent
# per-player evidence + constraint vs a joint pair head.
